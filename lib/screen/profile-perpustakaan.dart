import 'package:flutter/material.dart';

class ProfilePerpustakaan extends StatelessWidget {
  const ProfilePerpustakaan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Profil Perpustakaan'),
      ),
    );
  }
}
